model Boss {
    id              Int        @id @default(autoincrement())
    siret           String     @unique @db.VarChar(20)
    password        String     @db.VarChar(255)
    firstName       String?
    photo           String?
    backgroundImage String?
    computers       Computer[] @relation("BossComputers")
    employees       Employee[]
}
